module Hemingway
  grammar Special

    # Special characters that need to be escaped in Latex. 
    # Example: War \& Peace
    rule special
      escape character:( "#" / "$" / "%" / "&" / "_" ) {
        def html
          character.text_value
        end
      }
    end

    rule escape
      "\\"
    end

  end
end